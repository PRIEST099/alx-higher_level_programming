#!/bin/bash

# Specify the directory where you want to search for files
directory="/path/to/your/directory"

# Use 'find' to locate all files in the directory (excluding subdirectories)
find "." -type f -maxdepth 1 | while read -r file; do
    echo "File: $file"
    cat "$file"
    echo "---------------------------------------"
done

